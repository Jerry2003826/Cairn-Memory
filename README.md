# OmniMemory

Week-1 vertical slice for a local memory loop:

```text
Claude Code run
→ redacted trace
→ deterministic facts
→ generated memory block
→ measurably changed behavior in the next run
```

This repository follows the scope and safety boundaries in `AGENTS.md`.
